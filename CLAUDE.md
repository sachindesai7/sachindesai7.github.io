# CLAUDE.md

This file points Claude Code to the shared agent onboarding.

**Read [`AGENTS.md`](AGENTS.md) first, then [`MEMORY.md`](MEMORY.md).**

All project rules, conventions, and the required change-workflow live in `AGENTS.md`.
Everything in there applies to Claude Code too — there is no separate ruleset.

Quick reminders:
- Static site (HTML/CSS/vanilla JS), no frameworks, no build step.
- Theme + video + mobile rules are in `AGENTS.md` §3.
- After any change: update `context/*.md` + `MEMORY.md` date, then commit & push to `main`.
- Commits use Sachin's personal identity (set locally) — see `AGENTS.md` §5.
